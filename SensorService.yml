server:
  port: 8085

spring:
  application:
    name: sensor-telemetry-service
  datasource:
    url: jdbc:mysql://localhost:3306/sensor_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Ijse@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

automation-service:
  url: http://localhost:8083/api/automation/process

# Credentials for external IoT API mock
iot:
  api:
    username: admin
    password: password123